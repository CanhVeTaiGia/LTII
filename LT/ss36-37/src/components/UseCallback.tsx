export default function UseCallback() {
  return (
    <>
      {/* 
      Đây là 1 hook trong ReactJS
      Cách dùng
      import từ React
      useCallback: nhận vào 2 đối số: 
      1. callback là arrow function
      => xử lí logic
      2. [] || [dependency]
      => khi nào dependency thay đổi thì useCallback được goi
      <=> như useEffect
    */}
    </>
  );
}
